spring:
  mail:
    host: smtp.gmail.com
    port: 587
    username: ${MAIL_USERNAME:}
    password: ${MAIL_PASSWORD:}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

job-digest:
  user: sergey.moskovskiy@gmail.com
  recipients:
    - "sergey.moskovskiy@gmail.com"

gmail:
  credentials-file: classpath:credentials.json
  tokens-directory: tokens